import { Paper, Typography } from "@mui/material";

export default function WorkerDashboard(){
  return (
    <Paper sx={{ p:3 }}>
      <Typography variant="h5">Worker Dashboard</Typography>
      <Typography>Overview of workers</Typography>
    </Paper>
  )
}
